# ================================================================
# KOBONG VPN BOT — Environment Configuration
# Copy this file to .env and fill in real values. NEVER commit .env
# ================================================================

# ---------------- Telegram Bot ----------------
# Get token from @BotFather
BOT_TOKEN=1234567890:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA

# Bot username (without @) — displayed in help messages
BOT_USERNAME=KobongVpnBot

# Comma-separated Telegram user IDs of super admins (get from @userinfobot)
SUPER_ADMIN_IDS=123456789

# Optional: chat ID to receive system notifications (errors, new orders)
NOTIFY_CHAT_ID=

# ---------------- Encryption ----------------
# 32-byte URL-safe base64 key used to encrypt VPS credentials at rest.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# WARNING: If you lose this key, all stored VPS passwords are unrecoverable.
FERNET_KEY=CHANGE_ME_TO_A_REAL_FERNET_KEY

# ---------------- Database ----------------
# SQLite (default, good for < 5000 users). Path is relative to bot working dir.
DATABASE_URL=sqlite+aiosqlite:///data/kobong.db

# For Postgres, use:
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/kobong

# ---------------- Pakasir Payment Gateway (polling-based) ----------------
# No webhook / no public domain needed — the bot polls Pakasir every 10s
# until payment completes. See docs/PAYMENT.md for details.
#
# Register at https://app.pakasir.com and create a project.
PAKASIR_SLUG=your-project-slug
PAKASIR_API_KEY=your-api-key

# Default payment method for QR display: qris (recommended) / bri_va / bni_va / etc.
PAKASIR_METHOD=qris

# Only override for staging/testing. Leave default for production.
PAKASIR_BASE_URL=https://app.pakasir.com

# ---------------- Behavior ----------------
# Default currency label (display only)
CURRENCY=Rp

# One-time install fee (Rupiah) charged when a reseller triggers install
# on their own VPS. After paying this once per VPS, they can create
# UNLIMITED SSH/VMess/VLESS/Trojan/Shadow/ZIVPN accounts for FREE.
# Super admin is exempt from this fee.
DEFAULT_PRICE_INSTALL=50000

# Marketplace prices (v2 feature — unused for now).
# For users who don't have their own VPS and want to buy accounts on
# a shared VPS provided by the super admin.
DEFAULT_PRICE_SSH=5000
DEFAULT_PRICE_XRAY=8000
DEFAULT_PRICE_ZIVPN=10000

# Default account duration (days)
DEFAULT_DURATION_DAYS=30

# Maximum concurrent SSH sessions per bot to prevent VPS overload
SSH_POOL_SIZE=10

# Log level: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO
